/*
 * Company: Twitter
 * Title: Histogram of Tweets
 * 
 * Assume you're given a table Twitter tweet data, write a query to obtain a histogram of tweets posted per user in 2022.
 * Output the tweet count per user as the bucket and the number of Twitter users who fall into that bucket.
 * 
 * In other words, group the users by the number of tweets they posted in 2022 and count the number of users in each group.
 */

-- Create table "tweets"
create table tweets(
	tweet_id integer primary key,
	user_id integer,
	msg text,
	tweet_date timestamp
);

-- Insert sample values into "tweets" table
insert into tweets
values
	(241425, 254, 'If the salary is so competitive why won’t you tell me what it is?', '2022-03-01 11:00:00'),
	(214252, 111, 'Am considering taking Tesla private at $420. Funding secured.', '2021-12-30 00:00:00'),
	(739252, 111, 'Despite the constant negative press covfefe', '2022-01-01 11:00:00'),
	(846402, 111, 'Following @NickSinghTech on Twitter changed my life!', '2022-02-14 11:00:00'),
	(231574, 148, 'I no longer have a manager. I can''t be managed', '2022-03-23 11:00:00');

-- Solution
select
	tweet_count tweet_bucket,
	count(tweet_count) users_num
from (
	select count(user_id) tweet_count
	from tweets
	where date_part('year', tweet_date) = '2022'
	group by user_id
) tweet_count_table
group by tweet_count;
